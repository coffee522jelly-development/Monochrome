# PDF出力の整合性に関する技術レポート

## 1. 現状の課題と原因分析
現在の「PDF保存」機能は、`html2pdf.js`（内部で `html2canvas` と `jsPDF` を使用）を利用しています。この方式で「画面とPDFの結果が一致しない」主な原因は以下の通りです。

- **Canvas経由のラスタライズ:** `html2canvas` はDOMを擬似的にキャプチャして画像化します。この際、ブラウザのレンダリングエンジンと完全に同じ結果を得るのが難しく、特に複雑なMermaid図面（SVG）やWebフォントのメトリクス（文字幅）で微細なズレが生じます。
- **解像度（DPI）の不一致:** 画面表示（96DPI等）とPDFの内部座標系、およびラスタライズ時のスケール設定が干渉し、図面の縦横比や線の太さが変わって見えることがあります。
- **CSSの解釈差:** Canvas描画エンジンは、すべての最新CSSプロパティ（特にFlexboxや複雑なグリッド、カスタム変数の一部）を完璧にサポートしているわけではありません。

## 2. 整合性を確保するための解決策

画面で見ている内容を「そのまま」PDFにするための、3つのアプローチを提案します。

### 案A：ブラウザ標準印刷機能の活用（Native Print）
**[推奨・最も確実な手段]**
ブラウザ自身の印刷エンジン（Chrome/Edge/Safari等）を直接使用します。

- **メリット:** 1:1の整合性が保証されます。MermaidのSVGもベクターデータのまま高品質に出力されます。
- **実装方法:** `@media print` を使用して、印刷時のみエディターやサイドバーを非表示にし、プレビュー画面をA4全画面で表示させます。
- **操作感:** `Ctrl + P` または専用ボタンからブラウザの印刷ダイアログを呼び出し、「PDFとして保存」を選択します。

### 案B：Paged.js による組版制御
**[プロフェッショナルな冊子作成向け]**
W3Cの「CSS Generated Content for Paged Media」仕様に準拠した `Paged.js` を導入します。

- **メリット:** ページ番号、ヘッダー・フッターの固定、洗練された改ページ制御が可能になります。
- **デメリット:** ライブラリの重量が増え、設定がやや複雑になります。

### 案C：サーバーサイド・レンダリング（Puppeteer等）
**[エンタープライズ・自動化向け]**
Node.js サーバー上で Headless Chrome を動かし、`page.pdf()` を実行します。

- **メリット:** ユーザーのブラウザ環境に依存せず、完全に同一のPDFを生成できます。
- **デメリット:** サーバー環境（バックエンド）が必要になり、Webアプリ単体での完結が難しくなります。

---

## 3. 今後の対応方針（本プロジェクトへの適用）

まずは**「案A：Native Print（ブラウザ印刷の最適化）」**を実装し、現在の Canvas 方式の限界を突破することを提案します。

1.  **印刷用スタイルの定義:** 印刷時のみプレビュー領域を A4 サイズに固定し、等幅フォントや Mermaid 図面が最適に配置されるよう CSS を調整します。
2.  **専用ボタンの設置:** 「PDF保存（標準印刷）」ボタンを追加し、ワンクリックで最適な設定の印刷プレビューを呼び出せるようにします。
3.  **Canvas方式の併用:** 従来の `html2pdf.js` は「画像として手軽に保存したい」場合のために残しつつ、精度を求める場合は「標準印刷」を推奨するハイブリッド構成にします。
